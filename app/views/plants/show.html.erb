<h1>List of Plants</h1>
<div class="media">
  <div class="media-body">
    <table class="table table-hover">
      <tr>
        <th>Name</th>
        <th>Medium</th>
        <th>Fertilizer</th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
      </tr>

    <% @plant.tasks.each do |task| %>
      <tr>
        <td><%= @plant.name %></td>
        <td><%= @plant.medium%></td>
        <td><%= @plant.type_of_fertilizer %></td>
        <td><%= link_to 'Show', plants_path(id: @plant), class: 'btn btn-mini btn-info' %></td>
        <td><%= link_to 'Edit', edit_plant_path(id: @plant), class: 'btn btn-mini btn-success' %></td>
        <td><%= link_to 'Destroy', plant_path(id: @plant), class: 'btn btn-mini btn-danger', :confirm => 'Are you sure?', :method => :delete %></td>
      </tr>
    <% end %>
    </table>
  </div>
</div>



<%= link_to "Add A Task For This Plant", new_plant_task_path(@plant) %>
<%= link_to "Edit This Plant", edit_plant_path(id: @plant) %>
<%= link_to "Delete This Plant", @plant, method: :delete, data: {confirm: 'Are you sure you want to delete this plant?'} %> 

