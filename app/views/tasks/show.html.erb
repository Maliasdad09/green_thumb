<p id="notice"><%= notice %></p>

<h1>Listing Plant's Tasks:</h1>

<div class="media">
  <div class="media-body">
    <table class="table table-hover">
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th>Time</th>
        <th>Price</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>

    
      <tr>
        <td><%= @task.name %></td>
        <td><%= @task.description %></td>
        <td><%= @task.time %></td>
        <td><%= @task.price %></td>
        
      </tr>
  
    </table>
  </div>
</div>


<%= link_to 'Edit', edit_plant_task_path(@task.plant, @task), class: 'btn btn-mini btn-success' %> |
<%= link_to 'Back', plant_tasks_path(@task.plant), class: 'btn btn-mini btn-primary' %>

